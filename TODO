Reorganize the movie info pages

Fix the bugs on the recommendation engine.

Make a better profile page?

Fix the user profile image not being saved (?? Could not reproduce)

A popcorn and a kernel icon would be nice? Maybe just a poof cloud animation for the transition between corncob and popcorn. 

Add an animation for collapsing the comment box. I want o

On the homepage: can a movie the user has liked be recommended to them?

Make a movie synch feature in the chat? would be pretty easy. I'm thinking a command like !sync
